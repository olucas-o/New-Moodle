{{!
  Frontpage — Instituto Celebra
  Página inicial estilo Netflix
  NÃO possui navegação de retorno
}}

<div class="celebra-frontpage">

  {{! =========================
      MEUS CURSOS
      ========================= }}
  <section class="celebra-section">
    <h2 class="celebra-title">Meus Cursos</h2>

    <div class="celebra-carousel">
      {{#mycourses}}
        {{> theme_celebra/course_poster }}
      {{/mycourses}}

      {{^mycourses}}
        <div class="celebra-empty">
          Você ainda não está matriculado em nenhum curso.
        </div>
      {{/mycourses}}
    </div>
  </section>

  {{! =========================
      RECOMENDADOS
      ========================= }}
  <section class="celebra-section">
    <h2 class="celebra-title">Recomendados</h2>

    <div class="celebra-carousel">
      {{#recommended}}
        <a href="{{buyurl}}" class="celebra-poster celebra-poster--small">
          <img src="{{image}}" alt="{{fullname}}" loading="lazy">
          <div class="celebra-meta">
            <h3 class="celebra-course-title small">{{fullname}}</h3>
          </div>
        </a>
      {{/recommended}}
    </div>
  </section>

</div>
